# Base URL of the DevWrapped Cloudflare Worker API.
# For local dev: http://localhost:8787
VITE_API_BASE=http://localhost:8787

# Set to 1 to render the deck from built-in mock data (no GitHub token / network needed):
# VITE_USE_MOCK=1
