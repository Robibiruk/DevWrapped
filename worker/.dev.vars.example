# Copy this file to `.dev.vars` (gitignored) and paste in a GitHub token.
# Create one at https://github.com/settings/tokens — classic token, no scopes needed (public data only).
GITHUB_TOKEN=Your_github_token
